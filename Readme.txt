To Compile : g++ main.cpp -o main.out

To run : ./main.out

After that code will ask for number of slots and after that code will ask for blocks in which you have to provide a sequence of inputs as block reference string.
